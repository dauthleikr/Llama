import("kernel32.dll") long GetStdHandle(int handleType)
import("kernel32.dll") bool WriteConsoleA(long handle, cstr str, int count, int numWritten, long null)

int Main()
{
	WriteConsoleA(GetStdHandle(-11), "Hello World!", 12, 0, 0);
	return 0;
}